version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - pip3 install pipenv
      - pipenv install
      - pip3 install -r requirements.txt
run:
  command: pipenv run python -m streamlit run app.py
  network: 
    port: 8501